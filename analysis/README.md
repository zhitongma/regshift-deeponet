# Analysis utilities

See the [E0-E11 analysis index](../docs/REPRODUCING.md#analysis-index) for purpose, inputs and reproduction limits.

Training uses `../scripts/run_experiment.py`; no old shell launcher or personal server directory is required. Final result tables are under `../data/frozen_results/`. Most field-level analyses need external data/checkpoints or a user-supplied model manifest.
